AGENTS.md
dist
node_modules
.env
pipeline.log
racingapi.txt
typeerror.txt
tools/lgbm_benchmark/.venv
tools/lgbm_benchmark/data

# canvas de design: só os arquivos-fonte, não o pacote publicado
design/*.html
!design/*.dc.html
__pycache__/

# Permissões locais do Claude Code — pessoais, não versionar.
# Os AGENTES (.claude/agents/) vão versionados de propósito: são contrato de
# trabalho do projeto, não preferência de máquina.
.claude/settings.local.json
